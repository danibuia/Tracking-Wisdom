import { FC } from "react";
import { Header, SideDrawer } from "../../../components/common";

const Home: FC = (): JSX.Element => {
  return (
    <>
      <Header />
      <SideDrawer />
    </>
  );
};

export default Home;
